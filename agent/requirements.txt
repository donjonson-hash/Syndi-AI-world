# agent/requirements.txt
# Зависимости для agent_deepseek
# Установка: pip install -r requirements.txt

# DeepSeek через openai-совместимый API (обязательно >= 1.0)
openai>=1.0.0

# GitHub API
PyGithub>=2.1.1

# FastAPI + сервер
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Переменные окружения
python-dotenv>=1.0.0

# HTTP-клиент
httpx>=0.27.0

# Pydantic (для FastAPI)
pydantic>=2.0.0
