# SyndiAI Backend — Core dependencies (MVP demo)

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation
pydantic==2.6.4
pydantic[email]==2.6.4
email-validator==2.1.0

# Database
sqlalchemy==2.0.23
aiosqlite==0.19.0
asyncpg==0.29.0

# Redis (кэш, сессии, pub/sub для аватаров) — redis[asyncio] даёт aioredis-совместимый API
redis>=5.0.0

# HTTP Client (нужен aiohttp для агентов)
aiohttp==3.9.5

# Utilities
python-dotenv==1.0.1
aiofiles==23.1.0

# Auth (JWT + password hashing)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt<5.0.0

# LLM client (OpenAI SDK — используется с Mimo API / base_url override)
openai>=1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# --- Опциональные (раскомментировать когда нужно) ---
# aiogram>=3.0.0         # Telegram bot
# apscheduler==3.10.4    # Планировщик задач
# alembic==1.12.1        # DB миграции
# black==23.11.0         # Форматтер
# flake8==6.1.0
# mypy==1.7.1
