# SyndiAI Backend — Core dependencies
# Install: pip install -r requirements.txt
# Torch CPU-only (saves ~2GB): pip install torch --index-url https://download.pytorch.org/whl/cpu

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation
pydantic==2.6.4
pydantic[email]==2.6.4
email-validator==2.1.0

# Database
sqlalchemy==2.0.23
aiosqlite==0.19.0
asyncpg==0.29.0

# Redis
redis>=5.0.0

# HTTP Client
aiohttp==3.9.5

# Utilities
python-dotenv==1.0.1
aiofiles==23.1.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt<5.0.0

# LLM client (OpenAI SDK / DeepSeek / GenAPI — compatible via base_url override)
openai>=1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Telegram Bot
python-telegram-bot>=20.0

# ─── Synthia: Semantic Search (embed_server.py) ───
# Torch: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
sentence-transformers>=3.0.0
transformers>=4.40.0
numpy>=1.26.0
tokenizers>=0.19.0
qdrant-client>=1.9.0

# ─── Опциональные (uncomment when needed) ───
# aiogram>=3.0.0
# apscheduler==3.10.4
# alembic==1.12.1
# black==23.11.0
# flake8==6.1.0
# mypy==1.7.1
